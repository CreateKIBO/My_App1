<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Top accent bar for equipped state (3dp, shown/hidden in code) -->
    <View
        android:id="@+id/v_equipped_top_bar"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:visibility="gone" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/md_outline_variant"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingHorizontal="14dp"
            android:paddingTop="18dp"
            android:paddingBottom="14dp"
            android:minHeight="200dp">

            <!-- Avatar emoji square (visible for AVATAR type) -->
            <FrameLayout
                android:id="@+id/layout_avatar_icon"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:visibility="visible">

                <View
                    android:id="@+id/v_avatar_bg"
                    android:layout_width="64dp"
                    android:layout_height="64dp" />

                <TextView
                    android:id="@+id/tv_emoji"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:textSize="32sp" />
            </FrameLayout>

            <!-- Theme preview rectangle (visible for THEME type) -->
            <FrameLayout
                android:id="@+id/layout_theme_preview"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:visibility="gone">

                <View
                    android:id="@+id/v_theme_bg"
                    android:layout_width="match_parent"
                    android:layout_height="64dp" />

                <View
                    android:id="@+id/v_theme_circle1"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="top|end"
                    android:layout_marginTop="8dp"
                    android:layout_marginEnd="10dp" />

                <View
                    android:id="@+id/v_theme_circle2"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="bottom|start"
                    android:layout_marginBottom="8dp"
                    android:layout_marginStart="10dp" />
            </FrameLayout>

            <!-- Name -->
            <TextView
                android:id="@+id/tv_item_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:letterSpacing="-0.01"
                android:maxLines="1"
                android:textColor="@color/md_on_surface"
                android:textSize="15sp"
                android:textStyle="bold" />

            <!-- Description -->
            <TextView
                android:id="@+id/tv_item_desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:lineSpacingMultiplier="1.3"
                android:maxLines="2"
                android:textColor="@color/md_on_surface_variant"
                android:textSize="11sp" />

            <!-- Price badge with mini coin -->
            <LinearLayout
                android:id="@+id/layout_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_price_badge"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:visibility="gone">

                <View
                    android:id="@+id/v_mini_coin"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginEnd="4dp"
                    android:background="@drawable/bg_mini_coin" />

                <TextView
                    android:id="@+id/tv_item_price"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:textColor="@color/coin_gold_dark"
                    android:textSize="12sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <!-- Free tag -->
            <TextView
                android:id="@+id/tv_free_tag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_price_badge"
                android:fontFamily="sans-serif-medium"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:text="免费"
                android:textColor="@color/coin_gold_dark"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone" />

        </LinearLayout>

        <!-- Corner status tag (overlaid on top-right of card) -->
        <TextView
            android:id="@+id/tv_status_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_tag_equipped"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.02"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </com.google.android.material.card.MaterialCardView>
</FrameLayout>